["timestamp]\nNAMESPACE: default\nSTATUS: deployed\nREVISION: 1\n```\n\n---\n\n## Step 6: Verify Deployment\n\nCheck that all pods are running and healthy:\n\n```bash\nkubectl get pods\n```\n\nExpected output:\n```\nNAME                              READY   STATUS    RESTARTS   AGE\ntodo-app-frontend-xxx             1/1     Running   0          30s\ntodo-app-backend-xxx              1/1     Running   0          30s\n```\n\nWait for pods to reach `Running` status (may take 1-2 minutes).\n\n**Watch pods status**:\n```bash\nkubectl get pods -w\n```\n\nPress `Ctrl+C` to stop watching.\n\n---\n\n## Step 7: Verify Services\n\nCheck that services are created:\n\n```bash\nkubectl get services\n```\n\nExpected output:\n```\nNAME               TYPE        CLUSTER-IP       PORT(S)        AGE\ntodo-app-frontend  NodePort    10.x.x.x         80:30080/TCP   1m\ntodo-app-backend   ClusterIP   10.x.x.x         8000/TCP       1m\n```\n\n---\n\n## Step 8: Access Application\n\nGet the URL for the frontend service:\n\n```bash\nminikube service todo-app-frontend --url\n```\n\nExpected output:\n```\nhttp://192.168.49.2:30080\n```\n\n**Open in browser**:\n```bash\n# macOS\nopen $(minikube service todo-app-frontend --url)\n\n# Linux\nxdg-open $(minikube service todo-app-frontend --url)\n\n# Windows\nstart $(minikube service todo-app-frontend --url)\n```\n\n---\n\n## Step 9: Verify Functionality\n\n1. **Application loads**: Browser shows Todo AI Chatbot UI\n2. **Authentication works**: Sign in/sign up functionality operates\n3. **Chat works**: Chatbot responds to user messages\n4. **Tasks work**: Create, read, update, delete tasks\n5. **Database persistence**: Tasks persist across page refreshes\n\n---\n\n## Step 10: Troubleshooting\n\n### Check Pod Logs\n\n**Frontend logs**:\n```bash\nkubectl logs -l app=todo-app-frontend\n```\n\n**Backend logs**:\n```bash\nkubectl logs -l app=todo-app-backend\n```\n\n### Check Events\n\n**Pod events**:\n```bash\nkubectl describe pod -l app=todo-app-frontend\n```\n\n**Service events**:\n```bash\nkubectl describe service todo-app-frontend\n```\n\n### Use AI Tools\n\n**Troubleshoot failing pods**:\n```bash\nkubectl-ai \"why are my pods failing?\"\n```\n\n**Create missing secret**:\n```bash\nkubectl-ai \"create secret for database connection\"\n```\n\n**Analyze cluster health**:\n```bash\nkagent \"analyze cluster health\"\n```\n\n**Check resource usage**:\n```bash\nkubectl-ai \"show me resource usage\"\n```\n\n### Common Issues\n\n**Issue: Image pull errors**\n- **Solution**: Load images into Minikube: `minikube image load todo-frontend:local`\n\n**Issue: Pod stuck in Pending**\n- **Solution**: Check resources: `kubectl describe pod <pod-name>` and verify Minikube has sufficient CPU/memory\n\n**Issue: Service not accessible**\n- **Solution**: Verify NodePort: `kubectl get service todo-app-frontend` and check firewall\n\n**Issue: Database connection errors**\n- **Solution**: Verify secret exists and is correct: `kubectl get secret todo-secrets -o yaml`\n\n**Issue: Pods keep restarting**\n- **Solution**: Check health probe logs: `kubectl logs <pod-name>` and verify `/health` and `/ready` endpoints\n\n---\n\n## Update Deployment\n\n### Modify Configuration\n\nEdit values:\n```bash\nhelm edit values todo-app\n```\n\n### Rebuild Image with New Version\n\n```bash\ncd frontend\ndocker build -t todo-frontend:v2 -f Dockerfile .\ncd ..\n```\n\n### Upgrade Deployment\n\n```bash\nhelm upgrade todo-app ./helm/todo-chart \n  --set frontend.image.tag=v2 \n  --set backend.image.tag=v2\n```\n\n### Rollback Deployment\n\n```bash\nhelm rollback todo-app\n```\n\n### Scale Deployment\n\n```bash\n# Scale backend to 3 replicas\nkubectl scale deployment todo-app-backend --replicas=3\n\n# Using AI tools\nkubectl-ai \"scale backend to handle more load\"\n```\n\n---\n\n## Cleanup\n\n### Uninstall Helm Chart\n\n```bash\nhelm uninstall todo-app\n```\n\n### Delete Secrets\n\n```bash\nkubectl delete secret todo-secrets\n```\n\n### Stop Minikube\n\n```bash\nminikube stop\n```\n\n### Delete Minikube Cluster\n\n```bash\nminikube delete\n```\n\n---\n\n## Advanced Usage\n\n### Access Kubernetes Dashboard\n\n```bash\nminikube dashboard\n```\n\n### Port Forwarding\n\n```bash\n# Forward backend port\nkubectl port-forward service/todo-app-backend 8000:8000\n\n# Forward frontend port\nkubectl port-forward service/todo-app-frontend 3000:80\n```\n\n### Exec into Pod\n\n```bash\n# Frontend pod\nkubectl exec -it $(kubectl get pod -l app=todo-app-frontend -o name) -- /bin/sh\n\n# Backend pod\nkubectl exec -it $(kubectl get pod -l app=todo-app-backend -o name) -- /bin/sh\n```\n\n### Resource Monitoring\n\n```bash\n# Top pods by resource usage\nkubectl top pods\n\n# Analyze with AI tool\nkagent \"check backend performance\"\n```\n\n---\n\n## Development Workflow\n\n### Local Development Loop\n\n1. Make code changes\n2. Build new image: `docker build -t todo-frontend:local -f Dockerfile .`\n3. Load to Minikube (if needed): `minikube image load todo-frontend:local`\n4. Upgrade deployment: `helm upgrade todo-app ./helm/todo-chart`\n5. Verify changes in browser\n\n### Using Docker AI (Gordon)\n\n```bash\n# Optimize Dockerfile\ndocker ai \"optimize my dockerfile for production\"\n\n# Check image size\ndocker ai \"why is my image so large?\"\n\n# Add health check\ndocker ai \"add health check to my container\"\n\n# Security review\ndocker ai \"secure my dockerfile following best practices", "--", "Support\n\nFor issues with:\n- **Docker**: https://docs.docker.com/\n- **Minikube**: https://minikube.sigs.k8s.io/docs/\n- **Kubernetes**: https://kubernetes.io/docs/\n- **Helm**: https://helm.sh/docs/\n\n---\n\n## Next Steps\n\n- Explore `/helm/todo-chart/` for configuration customization\n- Review `/contracts/` for API contracts\n- Check `/specs/004-kubernetes-deployment/` for detailed specifications\n- Run `/sp.implement` to generate Dockerfiles and Helm charts"]